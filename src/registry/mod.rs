pub mod traits;
pub mod pub_dev;
pub mod hatch_dev;
pub mod private;

pub struct RegistryManager {
    // TODO: Add registry management state
}

impl RegistryManager {
    pub fn new() -> Self {
        Self {}
    }
}